---
name: PD planes, Riemann Conv A/B, and the GILE-rename scope trap
description: Three distinct complex planes; which Riemann affine convention is canonical and why the two aren't inverses; the operational GILE=5(σ−0.5) mass-rename trap.
---

# Three complex planes are DISTINCT (resonance, not identity)
The corpus reuses an imaginary-axis motif (abstract→imag, manifest→real) in THREE
unrelated constructions; never unify them (no `GILE = MI = γ`):
1. Truth–Existence: `z = HEM + i·GILE` (real=HEM/Existence, imag=all-GILE/abstract-Truth).
   This SUPERSEDES the legacy `z = E + i·GIL` (urb_629). Apply the rename to Plane-1
   references ONLY.
2. PD plane: real = degree-of-truth, imag = MI/Tralse modality.
3. Riemann affine: real = σ-coordinate, imag = γ (zero height).
**Why:** sharing the motif is a TPS-1/RAI-1 resonance; asserting one plane = another is an overclaim.

# Riemann affine: Conv A is canonical; Conv A and Conv B are NOT inverses
- **Conv A (canonical):** `PD(s) = 5(σ−½) + i·γ/γ₁`, critical line at **PD = 0**, symmetric range [−2.5,+2.5].
- **Conv B (operational embedding):** `σ = (PD+3)/5` ⇔ `PD = 5σ−3`, puts σ=½ at **PD = −0.5**, asymmetric [−3,+2].
- They differ by a 0.5 PD shift — **B is NOT the inverse of A**. B70's "−0.5 = Riemann midpoint" belongs to Conv B (the operational-range embedding), not the canonical map.
**How to apply:** when anything places the critical line, it must say PD=0 (Conv A). Treat any "critical line at −0.5" text as the Conv-B operational embedding and label it so.

# The GILE=5(σ−0.5) mass-rename TRAP (scope guardrail)
`GILE = 5(σ−0.5)` appears ~30× in CODE (universal_time_standard.py, unified_biometric_manager.py,
oura_ring_integration.py, icell_shell_physics_dashboard.py, gile_5_multiplier_breakdown.py, etc.)
but in those files **σ = a coherence/accuracy metric, NOT Re(s)**. That is a *different application*
(coherence→GILE operational mapping), not the Riemann map.
**Why:** the author-ratified rename ("Riemann real part → PD-real/σ-coordinate, decoupled from GILE")
applies to the RIEMANN context only. Mass-renaming the coherence→GILE code paths would be wrong and
could break code.
**How to apply:** rename only Riemann-context "GILE=5(σ−0.5)"; leave coherence→GILE code untouched.

# Q4 honest-result pattern (reusable)
- Riemann affine on the 1M open Odlyzko zeros (`data/riemann_zeros/first_1million_zeros.txt`):
  Re(PD)=0 is BY CONSTRUCTION (file holds γ-heights on the assumed line) ⇒ NOT an RH test; the GUE
  spacing reproduction is scale-invariant (PD-image = γ-axis ÷ γ₁) and just reproduces Montgomery–Odlyzko.
- TSC ring-constant "signatures": always run a coincidence-rate control (random constant vs nearest
  ring). Result was 6.03% vs 9.19% chance, P(random beats)=0.437 ⇒ HEURISTIC not a validated law (HAN-1).
