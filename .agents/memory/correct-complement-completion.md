---
name: Correct-complement completion (i·i vs i·conj)
description: Why a tralsity completes to truth only against its correctly-recognized complement, and the honesty rails on the i↔tralse reading.
---

# Correct-complement completion — truth from MR-reconciliation, not self-doubling

**Core lesson:** a tralsity does NOT complete to truth by interacting with a *copy of itself*. Self-multiplication gives the **false pole**; only the **correctly-recognized complement**, reconciled via **MR (Myrion Resolution)**, gives the **true pole**.

- Ordinary math: `i·i = i² = −1` vs `i·(−i) = i·ī = |i|² = +1`; generally `z·z̄ = |z|² ≥ 0` for all z, while `z·z = z²` stays rotated. i is the *paradigm* case, not the only one — credit that this bare distinction is already standard/in-corpus (EVD-1: no over-credit).
- **Truth↔+1 / falsity↔−1 is a framework GLOSS, not a theorem** (the i↔tralse identity is a candidate; the Myrion-Resolution instantiation is a *conditional* theorem — the "1 = truth" reading is the identity in miniature). Never state it as a corollary.
- "i sees plain i in the mirror" = metaphor for **mis-recognition**; the *actual* conjugate of i already IS −i. Do NOT imply conjugation returns i.

**Durable framing lessons (avoid re-introducing old errors):**
- The correct-complement contrast (self vs mirror) is **real and decisive** — do NOT call it a "false dichotomy." The honest picture is "rotation layered on a recognition": recognize the right complement → truth directly, OR pass through the −1 pole and let i's rotation carry you home (so −1 is not a dead end).
- Watch for the imprecision "i turns on itself and returns to unity" — *turning on itself* is `i·i = −1`, not `i·(−i)`.

No new mechanism/label/pipeline/falsifier; count stays 80. Home concepts: MR Truth Labels (Myrion Resolution), i-as-deficit-in-reality (DTR-1), the i-as-tralseness reading in the mathematics chapter.
