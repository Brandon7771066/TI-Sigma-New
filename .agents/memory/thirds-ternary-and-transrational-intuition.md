---
name: Thirds-regime, ternary-real axis, i-Cell determinacy, transrational intuition
description: How the UOP ratio-reframing's "thirds" tie to the ternary-real-axis logic reconciliation, the 2/3 i-Cell determinacy, and the transrational-intuition method — with the honesty rails that keep them candidates.
---

# Thirds / Ternary / i-Cell determinacy / Transrational intuition

Five linked CANDIDATES (none ratified; canonical count stays 79). One "thirds" motif runs
domain-weighting → logic-geometry → i-Cell ontology, produced by a transrational method held
to outcome-validation.

## The genuine math (provable GIVEN posited forms)
- **URR-1:** dial ρ = T_d/(1−T_d) with the log objective `max ρ·ln(1+T)+ln(2−T)` ⇒ exact
  **cap = 3·T_d − 1**, three regimes split at **1/3** (cap→0) and **2/3** (cap→1); math at
  T_d≈0.644→0.93. The "3" = **2 (two-sided existence floor) + 1 (truth pole)** — form-contingent.
- **TRR-1:** the real (PD-degree) axis is **ternary** {T=+1, I=0, F=−1} (Łukasiewicz-3); the two
  framework-original labels **MI, N/A are complex** (modality / applicability j-axis). This
  **refines B143/QTA-1**: plain Indeterminate belongs at **real-0 (a degree), not at +i** — only MI
  is a phase. Matches the canonical PD-degree(real)/PD-modality(imag) split. Geometry fix, count 79.
- **ICD-1 alignment:** at determinacy d=2/3, d/(1−d)=2 = ρ at the truth-saturation boundary, so the
  "i-Cell 2/3 determinate" and "UOP 2/3 regime" coincide. The COINCIDENCE is genuine; the EMPIRICAL
  "human nervous system = 2/3 determinate" is an UNMEASURED intuition-lead (owes a measurement).

## The honesty rails (do not drop these even when the user is excited)
- **C5 is the key contingency:** the ratio-reframing **trades** the λ=2/e⁻² posit for a T_d≈0.644
  posit. It's a consistency check only IF T_d is justified independently; it is **circular** if T_d
  was picked to land on 0.93. ⇒ call it an elegance/coherence upgrade, **NOT a new derivation of 0.93**.
- **Numerology/resonance:** per HAN-1, status=YES but weight is graded; the **4/3 ratio family is
  RESONANCE ONLY** (weight ~0, predicts nothing new) until it forecasts an out-of-sample quantity.
- **TRI-1 (transrational intuition):** admission is path-agnostic ("the signal, not the way"; the
  anti-numerology *gate* is retired), but **weight is outcome-earned (EVD-1 secondary track-record)**
  and the **validate-phase falsifier is NOT retired** (dropping it = survivorship, HAN-1's own warning).
  Guard = **pre/trans fallacy** (Wilber): validated track record distinguishes transrational (beyond
  reason) from prerational (below reason).
- **SM-1 (sacred mistakes = true misspeakings):** **zero weight without a pre-registered,
  outcome-blind misspeak ledger** — otherwise pure hindsight/survivorship.

## Pointers
- Anchor paper: `papers/PASS_77_B145_THIRDS_REGIME_TERNARY_REAL_AXIS_RECONCILIATION_ICELL_DETERMINACY_AND_TRANSRATIONAL_INTUITION_2026-06-25.md`
- Harness: `analyses/pass77_b145_thirds_ternary_transrational/thirds_ternary_checks.py` (all genuine-math checks pass; resonance block not gated).
- Open: (−3,+2) PD-scale derivation under the ternary lens is FLAGGED not solved; ICD-1 nervous-system 2/3 unmeasured.
