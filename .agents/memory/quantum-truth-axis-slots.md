---
name: Quantum slots for the 4 Truth Axes + polar GILE/HEM cross-linkage (QTA-1)
description: How the four TI Sigma Truth Axes map to concrete qubit objects, what is STRUCTURAL vs OVERLAY, and the anti-numerology guard that keeps it honest.
---

# QTA-1 — Quantum Truth-Axis Instantiation (CANDIDATE, not ratified; count stays 79)

The author's request "give GILE/HEM/the Truth Axes a physical mathematical slot,
+ a quantum basis or compelling analogy" is honestly satisfiable ONLY as a faithful
encoding + apt analogy — NOT as a physical-instantiation or quantum-reality claim.
The deliverable is "or compelling analogy" (which the author explicitly allowed).

## The four axes, each with a concrete quantum slot
Truth = a single qubit `|ψ⟩ = cos(θ/2)|T⟩ + e^{iφ} sin(θ/2)|F⟩`; labels
{T,F,I,MI} = {+1,−1,+i,−i} = 4th roots of unity on the C4 plane.
- **A1 PD-degree ↔ Bloch polar angle θ.** degree = Born `Pr(True)=cos²(θ/2)`;
  poles = certain T/F, equator = max indeterminacy. (one polar DOF)
- **A2 PD-modality ↔ Bloch azimuthal phase φ.** I at +π/2, MI at −π/2. KEY apt
  fact: a T/F (Z) measurement is BLIND to phase (I,MI both give Pr=0.5, ⟨Z⟩=0);
  phase is recoverable ONLY by a rotated Y-probe (⟨Y⟩=+1 vs −1). This is exactly
  modality⊥degree. **Carries the live empirical bet QTA-1-F1.**
- **A3 τ/δ separability ↔ tensor-product (Schmidt-rank-1) separability.** TJ=τ·δ;
  product state (concurrence 0) ⇒ τ,δ independently recoverable; entangled (Bell,
  concurrence 1) ⇒ τ undefined alone. "separable" read LITERALLY = factorizes.
- **A4 Authority Axis ↔ measurement context / contextuality (CHSH).** context-free
  (global, authority-independent) verdict capped at **2**; genuine truth-state hits
  **2√2≈2.828** ⇒ AA cannot reduce to a context-free label. Ties to canon:
  **Fine 1982 (joint-measure ⇔ CHSH) == UOP B133 Contextual Admissibility.**

## Cross-linkage (HEM ↔ 64D matrix) = the polar form (STRUCTURAL)
`z_k = r_k·e^{i·a_k}`: r_k = HEM modulus (existence), a_k = GILE phase (valence) —
one complex spinor amplitude per index. It IS `z=r·e^{iθ}`, so it's an identity,
and it EXPLAINS why the B137 bolt-along-the-GILE-index is natural (binds each
modulus to its phase, not ad hoc). **8 = 4 moduli (HEM) + 4 phases (GILE).**

## The honest spine — STRUCTURAL vs OVERLAY (the #69 rail)
- STRUCTURAL/earned-analogy: label geometry, 8=4+4 DOF (Dirac), the qubit/Bloch/
  contextuality slots, the polar cross-linkage.
- OVERLAY (mnemonic, ZERO evidential weight): the 8 HEM-GILE ↔ 8-constant
  assignment {0,1,i,√2,e,φ,π,C}. Recorded result: natural map corr **0.075**,
  permutation **p=1.0** (B135/B138); 4 points can't reach p<0.05 (floor ≈0.08).
- **ANTI-NUMEROLOGY GUARD (the decisive move — reuse this pattern):** the demo
  SHOWS every quantum slot is *independent of the constant assignment* (re-derive
  with the GILE phases scrambled to arbitrary values → all checks still exact). So
  the structural content borrows NO credibility from the constant overlay. **Why:**
  this cleanly separates the earned part from the forbidden numerology in one test.
  **Apply:** whenever the author wants "constants = dimensions," grant the slot's
  structure, prove it stands without the constants, keep the constant map as a lens.

## What this does NOT claim (rails held)
No physical instantiation, framework-is-quantum, constant-IS-dimension, or moral
realism proven. Falsifiers: **QTA-1-F1** (real rater data: I vs MI inseparable on a
T/F probe yet separable with a modality/leeway probe — refuted if T/F alone splits
them), **QTA-1-F2** (AA needs genuine authority-frame context-dependence else it's a
non-contextual feature), **DCI-1-F1** (constant overlay must predict a NEW relation,
>4 pts, outcome-blind, p<0.05). Demo: `analyses/pass77_b143_quantum_truth_axes/`
(deterministic, 12 exact checks, no load-bearing number). Canon for the axes/labels:
`.agents/memory/gile-64d-matrix-axes.md`. Constants overlay: `na-hyperimaginary-and-moral-realism-case.md`.
