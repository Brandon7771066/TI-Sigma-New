---
name: Tetrad–Truth Identity, Consciousness-as-Ought, Cross-Normativity
description: How to handle the C4↔truth-label mapping and the "math dissolves Hume" debate honestly in this corpus.
---

## The C4 i-Cell carries TWO charts — never conflate
- Same group `{1,i,−1,−i}` is mapped BOTH to GILE `{G,I,L,E}` (URB_371/670) AND to base-4 truth labels `{1=True, −1=False, i=Indeterminate, −i=MI}`. They are two coordinate charts on one group, not one claim. Demarcate (cf. Myrion overload).

## The tetrad↔truth mapping is grounded, not numerology — but still only an overlay
- It is FORCED by two already-canonical facts, so it is NAD-1-defensible (not post-hoc): real axis = determinate truth (`QUANTUM_LOGIC_DIMENSIONAL_RECONCILIATION.md` already fixes 1=True/−1=False) + imaginary axis = MI/Tralse modality (refinement #8 / PDR-1). The `+i`/`−i` split follows the leeway demarcation (refinement #17: leeway present=Indeterminate, annihilated=MI); conjugation = "flip indeterminacy modality."
- **ERRATA to know:** the old `QUANTUM_LOGIC` paper says `i = MI` (3-valued, pre-base-4) — coarse/legacy. Current label for `+i` is **Indeterminate**; MI is `−i`. Read the old paper as superseded on this point.
- **Why it's still not evidence:** grounded ≠ predictive. Like the B135 8-constant map (perm p=1.0), a mapping that only relabels is an overlay until it predicts a NEW out-of-sample fact. Keep operator readings honesty-tagged: `T→I→F` arc is suggestive; `F→MI→T` and `¬I=MI` are just-algebra, not argued.

## "Can math dissolve Hume's guillotine?" — the honest line
- **Don't over-claim either way (#69).** A VALUE-FREE construction cannot bootstrap value (B135 MORAL-F1) — true and narrow. But that does NOT entail "math can never dissolve Hume"; that stronger spirit was withdrawn. The author's route is coherent and NOT refuted by B135.
- **The author's route (CIO-1):** consciousness instantiates the ought from objectively-existing GILE values. It DISSOLVES Hume *conditional on* that bridge premise; it PRESUPPOSES rather than proves moral realism. So it RELOCATES the dispute from logic (inference-rule) to ontology (do the values exist) — which is genuine progress, and consistent with "gap relocated into the objective."
- **Headline to use:** "dissolved given the bridge premise," NOT "proven from nothing." Never write that moral realism is proven.
- **Over-claims to correct gently (EVD-1, not preachy):** "everything has been explained by math" → weaker true version = Wigner unreasonable-effectiveness + no domain shown in-principle un-modelable ⇒ reasonable PRIOR, not proof. "Math has preferences (φ, music)" → precise version = variational/extremal distinguished points; keep φ-in-music ILLUSTRATIVE only (anti-numerology).

## Cross-normativity synthesis (NB-1) — the clean way to say it
- **math = FORM** (variational/extremal template; physical law = "nature extremizes"; UOP `argmax ρ·f_cap(G)+g(H)` is that same shape for ethics, B133 Interior-Optimum), **objective GILE values = CONTENT**, **consciousness = FORCE** (turns a DESCRIPTIVE extremum "systems do extremize" into a PRESCRIPTIVE "agent ought to" — this is the seam where CIO-1 is needed; without it NB-1 over-proves, e.g. rocks "ought" to fall).

## Slogan
- "Building Impossible Bridges" = adopted TI Sigma tagline (slogan, NOT a principle). Rider that keeps it honest: every bridge must be EARNED via an open falsifier, never asserted.
