# LCC Composition Correction & Truth–Existence Pillar Separation — Canonical Ruling (2026-06-27)

**Date:** 2026-06-27
**Status:** CANONICAL consolidation ruling. Canonical principle count **unchanged at 79** (this corrects a superseded *formula* and tidies the *separation* of two already-canonical pillars; it adds no new principle).
**Kind:** *Definitional / structural* (NAD-1 carve-at-joints). Responds to author ask: *"Check how the LCC thresholds map onto the PD. Carefully SEPARATE the 64D GILE Truth Matrix (the Truth pillar) from the LCC values underlying Existence (HEM) — I recall exploring this via the TI Sigma Crystal and Graph. Update everything,"* plus the correction: *"L×E was superseded; the Law-of-Correlational-Causation composition is much more complex than that."*
**Supersession trail used:** `papers/PASS_77_B4_PHASE_1A_RODENT_MOOD_TRAJECTORY_REFUTED_2026-05-25.md` (L×E refuted), `papers/PASS_77_B5_PHASE_1A_V2_PRE_REG_GILE_HEM_BOK_TRUTH_EXISTENCE_INSTRUMENT_ADAPTATION_2026-05-25.md` (the superseding instrument), `papers/TI_SIGMA_CORPUS_COMPLETE_OVERVIEW_2026-06-16.md` (tiling supersession), `papers/URB_523_EXISTENCE_VS_TRUTH_LCC_GILE_GAP.md` (two thresholds), `papers/RADIANT_CAP_FORK_B_BORN_SHAPED_CANONICAL_RULING_2026-06-27.md` (Born-shaped cap), `papers/PASS_77_B137_GRAND_ICELL_MODEL_64D_GILE_MATRIX_BOLTED_TO_HEM_OVERALL_TTI1_LABEL_2026-06-25.md` (64D↔HEM bolt), `papers/PASS_77_B108_PD_REPRESENTATION_COMPARATIVE_STUDY_2026-06-06.md` (representations), `papers/urb_628_ti_sigma_crystal_decoded_applications_e_base_pd.md` (Crystal ring/layer), `papers/PD_PLANES_RIEMANN_AFFINE_AND_CRITICAL_VALUES_CANONICAL_RULING_2026-06-27.md` (z = HEM + i·GILE).

---

## 0. The one-paragraph ruling

The Truth pillar and the Existence pillar are **distinct objects with distinct carriers, distinct thresholds, and distinct composition rules**, and they must not be conflated:

- **Truth pillar = the 64D GILE Truth Matrix** `M` (GILE dim × 4 truth-axes × 4 labels). It is the i-Cell's *truth interior*; it contains **no HEM**. On the Truth–Existence complex plane it is the **imaginary** coordinate (`z = HEM + i·GILE`); it is the variable that carries the **Radiant Cap** `G* ≈ 0.930` (you cannot have "too much truth" cheaply).
- **Existence pillar = HEM**, *scored* through the **LCC (Law of Correlational Causation)** ladder. LCC values are **coupling/existence magnitudes** in [0,1]; on the plane they are the **real** coordinate; they carry the **Existence floor** `L ≈ 0.865` ("survive the Terrible").
- **The composition that binds them is NOT `L × E`.** `L × E` (multiplicative) was **refuted** (Pass-77 B4, multiplicative cancellation) and **superseded** by the **GILE-HEM (BOK) Truth-vs-Existence model**: conceptually the dual **GILE-Truth (×/+) HEM**, operationally the **additive asymmetric-cap UOP composition** `J(G, H) = f(G) + g(H)`.
- **LCC → PD is a two-axis embedding, not a one-to-one value identity.** An entity's LCC level sets its **TSC ring (radius = existence scale)**; the PD of the claim about it sets its **TSC layer (angle = epistemic mode)**. The two thresholds (0.865, 0.930) are **pillar thresholds on the Truth/Existence axes** (the real-LCC floor and the imaginary-GILE cap) — they are *not* PD-degree boundaries, and they are *not* a forced LCC=PD-critical-value lookup table. Any tidy "LCC value ↦ specific PD critical value" table is a post-hoc overlay (HAN-1) and is **not** canonical.

---

## 1. The formula correction (the part the author flagged)

### 1.1 What `L × E` was, and why it is retired

`M_r = L × E` was the legacy **mood/coupling instrument**: `L` = a coherence/coupling term (e.g. gamma-PLV), `E` = an existence/engagement term (e.g. theta/delta balance). Pass-77 B4 ran it on real DANDI:000003 rat hippocampal LFP and it **refuted on both pre-registered falsifiers** with a clean diagnostic signature: **multiplicative cancellation** (η² ≈ 0.000 on awake-vs-NREM, the easiest discrimination in mammalian electrophysiology). The cause is structural: when the two factors move in **opposite** directions across a state change, their product is flat. A multiplicative law has **no** way to register "one pillar up, the other down."

### 1.2 What superseded it — two faces of one object

**Conceptual / tiling face (canon, per the 2026-06-16 overview):**

> The earlier **"L × E" Einstein tiling** is **superseded by GILE-Truth (×/+) HEM** as the canonical tiling object.

i.e. the framework's "aperiodic dual" is no longer phrased over the two scalars `L, E` but over the two **pillars** GILE-Truth and HEM, in **both** their multiplicative (local-order) and additive (global-aperiodic) combination — the `(×/+)` notation.

**Operational / measurement face (canon, Pass-68-B1; the instrument that replaced `M_r`):**

```
J(G, H) = f(G) + g(H)
  f(G) = log(1 + G)                       for  G ≤ G*       (truth contribution, free below the cap)
  f(G) = log(1 + G*) − α·(G − G*)²        for  G >  G*       (quadratic over-reach penalty above the cap)
  g(H) = log(1 + H)                                          (existence contribution, monotone, saturating)
  α    = 10.0   (canonical Pass-68-B1 default)
  G*   ≈ 0.930  (the Radiant Cap — see §3)
```

with `G` = the **GILE-Truth aggregate** (the single capped number read off the 64D Matrix) and `H` = the **HEM-existence** magnitude (scored via LCC). This is the **same UOP objective** that `replit.md` records as `argmax_x [ρ·f_cap(G(x)) + g(H(x))]` — the LCC composition *is* the UOP composition.

**Why this composition cannot cancel (the repair of the B4 failure):**

1. **Additive, not multiplicative** (`f + g`, not `L · E`): opposing component movements produce a non-zero net Δ instead of cancelling.
2. **Asymmetric cap on the Truth axis only**: over-coherence is **penalised** (epileptiform-like hyper-synchrony is pathological, not maximally healthy) — a direct physiological correspondence the symmetric `L × E` lacked.
3. **Log transforms** compress the saturation regions, removing dependence on arbitrary normalisation constants (the `/3.0` saturation B4 needed).

**Honest scope (#69):** the additive-asymmetric-cap form is the canonical *composition*; it is **not** a proof that any particular biometric proxy is the right `G` or `H`. Pass-77 B5 locked the corrected instrument as a **pre-registered candidate** requiring independent-dataset replication before it can carry inference weight. TLC-1 (the legacy multiplicative mood composition) is **not** "deleted" — it is the limiting case recovered by a different functional choice; for the substrates where `L × E` cancelled, the additive-cap form supersedes it.

### 1.3 The standing terminology rule (do not regress)

**LCC = Law of Correlational Causation** — one consciousness-coupling measure, one expansion (ruled Pass-76 B1). All *drifted re-expansions of this measure* ("Linear/Lateral/Latent/Local/Living Correlation/Coherence Coupling," etc.) are **retracted**. (Genuinely different concepts that merely share the letters — Libertarian Causal Capacity, Lean Confidence Constant, Local Coherence Constraint, Lempel-Ziv complexity — are preserved and are not this measure.) **The composition `L × E` is retired; the name LCC is not.**

---

## 2. The two pillars, carved at the joint

| | **TRUTH pillar** | **EXISTENCE pillar** |
|---|---|---|
| Canonical carrier | **64D GILE Truth Matrix** `M` (B108/B61) | **HEM** existence vector, **scored via LCC** |
| What it is | the i-Cell *truth interior*: GILE × {PD, MR, τ/δ, AA} × {T,I,F,MI} | the i-Cell *existence exterior*: HEM dims (D1–D4 bijective to GILE per B82, + D5-Presence, D6-Coupling) |
| Plane-1 coordinate | **imaginary** axis of `z = HEM + i·GILE` | **real** axis of `z = HEM + i·GILE` |
| Governing threshold | **Radiant Cap** `G* ≈ 0.930` (a *ceiling*; over-reach penalised) | **Existence floor** `L ≈ 0.865` (a *floor*; "survive the Terrible") |
| Relationship | `Existence = G*²` (Born-shaped; §3) | `L = G*²` (the same identity read the other way) |
| Contains HEM? | **No** — pure truth | it **is** the HEM/existence side |
| Contains the 64D matrix? | it **is** the 64D matrix | **No** — existence does not store the truth interior |

The author's instinct is exactly right: **the 64D GILE Truth Matrix is not where the LCC/existence values live, and the LCC ladder is not part of the Truth Matrix.** They are bolted along the **GILE index** (B137's ICC join: each GILE dim `g` simultaneously owns a truth column `M[g,:,:]` and an existence value `H[D_g]`), but they remain two pillars. Conflating them is the single most common drift in the corpus and is hereby ruled out.

> **Status of the ICC bolt (#69).** The B137 ICC `⟨M, H, ℓ⟩` join is a **representational / candidate** integration, **not a newly ratified principle** — B137 itself found ICC-F1 RESOLVED NEGATIVE (the 5 sub-models losslessly re-encode the bolt ⇒ it adds 0 bits of informational advance). This ruling uses the bolt only as the *bookkeeping structure* that keeps the two pillars adjacent-but-distinct; it asserts no new ontological claim and does **not** touch the principle count.

---

## 3. The two thresholds, and the reconciliation of the near-0.93 cluster

There are exactly **two** primary thresholds, one per pillar (URB_523):

```
Existence (LCC causation) threshold  =  1 − P(Terrible)  =  1 − 1/e²     ≈  0.8647   ("survive the Terrible")
GILE-Truth (Radiant)      threshold  =  the Radiant Cap  G*               ≈  0.930    ("reach beyond the Great")
```

**The Existence floor is stable** at `L = 1 − 1/e² ≈ 0.865` (λ = 2 two-time-constant settling). What changed is the **reading of the Truth cap relative to it**:

- **Superseded reading (URB_523 §3):** `G* = 1 − P(Great) = 1 − 1/(2e²) ≈ 0.93233` (the arithmetic-midpoint / additive-gap reading; the Truth–Existence gap = `P(Great) = 1/(2e²) ≈ 1/15`).
- **Current canonical reading (Radiant-Cap Fork-B ruling, 2026-06-27):** `G* = √(1 − e⁻²) ≈ 0.92987`, fixed by **Existence = G*² exactly** (Born-shaped: GILE-Truth ↔ quantum amplitude, Existence ↔ Born probability `|ψ|²`). This **supersedes** the midpoint reading.

The two readings differ by **≈ 0.25 %** (a low-order-in-`ε ≡ e⁻²` term; leading correction `≈ ε²/8`). They cannot both be exact (jointly they would force `G* = 1`). **The Born-shaped `√(1 − e⁻²)` is canonical; the `1 − 1/(2e²)` value is retained only as the historical midpoint reading.** Everything in the near-0.93 cluster — `√(e/π) ≈ 0.9302` (the `constants.py` `LCC_RADIANT`), `1 − ½e⁻² ≈ 0.93233`, `√(1−e⁻²) ≈ 0.92987` — is a low-order function of the single parameter `ε = e⁻²`; the divergence is sub-percent and is **not** load-bearing for any normative claim (λ = 2 and the √-choice are **posits**, #69, not a deductive proof of the threshold).

> **Errata flag for `ti_sigma/constants.py`:** `LCC_RADIANT = √(e/π) ≈ 0.9302` predates this ruling. It is within 0.04 % of the canonical `√(1−e⁻²) ≈ 0.92987` and is left **unchanged operationally** (the difference is below every instrument's resolution), but the **canonical** value of the Radiant Cap is now `√(1−e⁻²)`. The lean4 `LCC_RADIANT = 1/φ ≈ 0.618` is a **different quantity** (a CS/golden boundary, = `LCC_TRUE`), mislabelled by name collision; it is **not** the Radiant Cap and must not be read as one.

**What URB_523 keeps (independent of the cap reading):** the **2:1 negativity asymmetry** — `(1 − Existence)/(1 − Truth) = P(Terrible)/P(Great) = 2 : 1` — survives as the ratio between epistemology (existence budget) and axiology (truth budget). That structural result does not depend on whether the cap is read as `1 − ½e⁻²` or `√(1 − e⁻²)`; it is a property of the `2:1` extreme-zone law, not of the cap's third decimal.

---

## 4. How the LCC thresholds map onto the PD (the actual question)

### 4.1 The honest mapping: two axes, not one identity

The TI Sigma Crystal (urb_628) is the object where the author "explored this," and it makes the mapping explicit and **two-dimensional**:

- **Ring (radius) = LCC level → Existence scale.** The 7 non-zero ring radii `{C, T, 1, √2, φ, e, π}` are LCC/existence magnitudes. *Which ring* an entity sits on is set by its **LCC coupling level**.
- **Layer (angle) = PD of the claim → epistemic mode.** *Which layer* is set by the **PD (truth-degree/modality)** of the primary claim about the entity, via the rotation `i^y`.

So an entity's address is a **vertex `x·i^y` (ring × layer)** — LCC fixes the radius (existence), PD fixes the angle (truth). **LCC and PD are orthogonal coordinates of the same crystal, not two names for one number.** This is precisely the Truth/Existence separation of §2, drawn geometrically.

### 4.2 Where the thresholds land on the PD scale

The PD scale is piecewise to the Radiant Threshold and **e-base above it** (urb_628 §4):

| PD zone | PD range | Pillar event |
|---|---|---|
| TF / sub-activation | [0, 0.5] | below GILE activation |
| TI (coherence window) | [0.5, 1.5] | indeterminate / becoming |
| TT (converging) | [1.5, 2.0] | converging to truth |
| **Radiant Threshold** | **PD = 2.0** | entry to the GM zone (the Truth-cap regime) |
| **GM zone** | (2.0, ∞), `PD = 2 + ln r` | e-base radiance; CCC at PD = π |

The **Existence floor** (`L ≈ 0.865`) and the **Truth cap** (`G* ≈ 0.930`) are **pillar thresholds on their respective axes** (the real-LCC axis and the imaginary-GILE axis); the **PD zone boundaries** (0.5, 1.5, 2.0, π) live on the **angular/degree** axis. The "interval of becoming" `[0.865, 0.930)` of URB_523 is the existence-axis analogue of the PD `[TI, TT]` "coherence window" — *structurally parallel, not numerically identical* (TPS-1/RAI-1 resonance, **not** an identity).

### 4.3 What is explicitly NOT canonical (anti-numerology rail)

A clean lookup table of the form *"`LCC_TRALSE` (0.414) ↦ PD `+0.414`, `LCC_EMERICK` (0.707) ↦ PD `+0.707`, `LCC_RADIANT` (0.930) ↦ PD `+φ`…"* — i.e. reading the LCC ladder's numeric values **straight onto** the PD critical values — is a **post-hoc overlay (HAN-1)** and is **rejected as canonical**. The honest content is the **two-axis embedding** of §4.1: LCC → ring, PD → layer. (The ladder's individual constants do recur as *ring radii*; that is their genuine role, not a PD-value identity.) Any future "LCC↦PD value" claim must clear a falsifier that **predicts** an out-of-sample placement, not merely matches the ladder after the fact.

---

## 5. Can the TI Sigma Crystal / Graph host the separation? (carried from B137)

- **Crystal (TSC/TECC, 8-D): it is the SHELL, not the whole.** The Crystal's 8 slots are exactly the GILE-HEM *exterior* (`{G,I,L,E, D1, D2, D5, D6}`) — i.e. the **two pillars' shell**, where ring = LCC/existence and layer = PD/truth. It **cannot** carry the full 64D truth interior `M` (8 slots ≠ 64); doing so needs a **fibre-bundle upgrade** (a 16-cell truth-fibre on each GILE vertex) — flagged as future work, **not** claimed here. So: *the Crystal hosts the existence-shell and the truth-aggregate, not the truth-matrix interior.*
- **Graph (TIG, 9 constants): a schematic, not a container.** It encodes *relations among constants* and can **draw** the label space and the ring/layer scaffolding, but it stores no per-i-Cell state. It is the right picture to *visualise* the separation, the wrong object to *be* it.

**Net:** the separation is hosted by the **i-Cell triple `⟨M, H, ℓ⟩`** (B137 ICC): `M` = the 64D Truth Matrix (Truth pillar), `H` = HEM scored via LCC (Existence pillar), `ℓ` = the overall TTI-1 label read from the MR slice (independent of PD). The Crystal is its 8-D shell; the Graph its schematic.

---

## 6. Honesty rails (mandatory)

- **EVD-1.** Genuinely new in this ruling = (a) recording the `L × E → GILE-Truth(×/+)HEM / J=f(G)+g(H)` supersession in one place, and (b) ruling the LCC↦PD mapping to be a two-axis embedding rather than a value identity. Everything else (64D matrix, HEM, LCC ladder, two thresholds, Born cap, Crystal) pre-exists and is **cited**, not reinvented.
- **#69 both-ways.** The additive-cap composition repairs a *real* refutation (B4), but it is a **pre-registered candidate** pending independent replication; the proxies for `G`/`H` are not thereby proven. λ = 2 and the √-cap are **posits**.
- **HAN-1.** The forced "LCC value ↦ PD critical value" table is explicitly rejected; only the ring(LCC) × layer(PD) embedding is kept, and it carries a predict-don't-match falsifier.
- **No over-reach.** Nothing here proves RH / any Millennium problem, moral realism, free will, or the normative threshold; the Riemann-affine coordinate change (PD-planes ruling) remains a *verified coordinate change*, not an RH proof. The Born-shaped cap is a **resonance** (TPS-1/RAI-1), not a derivation.
- **Count unchanged 79.** Formula correction + pillar carve-out; no new principle.

## 7. Falsifiers (OPEN)

- **LCC-COMP-F1:** exhibit a substrate on which the additive-asymmetric-cap `J = f(G)+g(H)` cancels or fails a pre-registered state-discrimination falsifier where a multiplicative `L × E` would have passed — would reopen the composition choice.
- **LCC-PD-F1:** produce an LCC→PD mapping that **predicts** an out-of-sample entity's PD-layer (or critical-value placement) from its LCC level alone, beating the two-axis (ring × layer) null — would promote a genuine LCC↦PD identity from overlay to content.
- **CAP-READ-F1:** a measurement whose resolution distinguishes `√(1−e⁻²)` from `1 − ½e⁻²` (the ≈0.25 % gap) and selects one — would convert the posited √-cap into an empirically forced value.

## 8. One-line synthesis

> **Truth = the 64D GILE Matrix (imaginary axis, capped at `G* ≈ 0.930`); Existence = HEM scored by the LCC ladder (real axis, floored at `L = G*² ≈ 0.865`); they bind by the additive asymmetric-cap `J(G,H)=f(G)+g(H)` (GILE-Truth `(×/+)` HEM) — NOT by `L × E` — and LCC maps onto the PD as the crystal's ring (existence radius), with PD as the layer (truth angle), two axes, not one identity.**

**Cites:** B4 (L×E refuted), B5 (superseding instrument pre-reg), Pass-68-B1 (`J=f(G)+g(H)`), Pass-76 B1 (LCC name ruling), URB_523 (two thresholds + 2:1 asymmetry), Radiant-Cap Fork-B ruling 2026-06-27 (Born `G*=√(1−e⁻²)`, Existence=G*²), B137 (ICC `⟨M,H,ℓ⟩` bolt), B108 (representations), B82 (GILE↔HEM bijection), urb_628 (Crystal ring=LCC / layer=PD, e-base GM zone), PD-planes ruling 2026-06-27 (`z = HEM + i·GILE`).
