================================================================================
SETUP — cap derived from T_d; features computed from the string alone
================================================================================
  G* = 0.93233 (= 3*T_d-1 at T_d=0.644111); no '0.93' typed.
  structural features: ['n_chars', 'n_words', 'n_distinct', 'compress_ratio', 'n_digits', 'neg_markers'] (none reads a proof/checker/label)

================================================================================
PART A — naive unbalanced real-math benchmark (the leakage trap)
================================================================================
  naive held-out accuracy = 0.924 (sd 0.087)
  the 'neg_markers' feature is doing the work: false statements happen to
  carry more negation/impossibility wording -> spurious structure<->truth link.
  PASS: P1: naive unbalanced benchmark accuracy > 0.70 (apparent signal)

================================================================================
PART B — negation-paired, polarity-balanced real-math corpus (leakage-free)
================================================================================
  mean per-pair token overlap (Jaccard) = 0.655 (near-minimal edits -> structurally near-identical members)
  PASS: P2a: paired members are near-identical (mean Jaccard >= 0.55)
  neg_markers mass on TRUE=4 vs FALSE=5 (balanced -> the PART A artifact is neutralized)
  balanced group-CV accuracy = 0.417 (sd 0.075) ~ chance 0.5
  PASS: P2: negation-paired balanced accuracy in [0.40, 0.62] (~chance)
  => with leakage controlled, NO surface structure tracks truth here.
     To label these you must actually do the mathematics. SFC-1-F1 NOT met.

================================================================================
PART C — decidable subclass (arithmetic): fidelity only via an evaluator/oracle
================================================================================
  surface-only accuracy   = 0.502 (no real signal from digits)
  +evaluator accuracy     = 1.000 (solves it — but the evaluator
                            IS the decision procedure / an oracle)
  PASS: P3a: arithmetic surface-only accuracy <= 0.65 (no structural signal)
  PASS: P3b: arithmetic + evaluator accuracy >= 0.95 (oracle feature; not M1)
  => SFC-1-BOUND escape hatch confirmed: fidelity is trivial on a decidable
     subclass ONLY because F there = the decider (zero predict-before-proof).

================================================================================
PART D — leakage tax (SFC-1-F3 live) and the SFC-1-F1 verdict
================================================================================
  leakage tax = naive 0.924 - balanced 0.417 = 0.507
  that gap was pure annotation artifact (negation-marker phrasing), not
  access to mathematical truth — a live demonstration of SFC-1-F3.
  PASS: P4: leakage tax (naive - balanced) > 0.15
  PASS: P5: SFC-1-F1 remains OPEN (no leakage-free non-oracular F predicted truth on the undecidable-flavored real-math corpus)
  HONEST SCOPE: small, hand-curated, ILLUSTRATIVE corpus. This is consistent
  with SFC-1-BOUND (no magic), NOT a census proving no F can ever exist, and
  NOT a claim about RH. SFC-1-F1 stays the open frontier (cf. Ramanujan
  Machine / Davies et al. Nature 2021 — real but heuristic generators).

================================================================================
ALL SFC-1-F1 ATTACK CHECKS PASSED
================================================================================
